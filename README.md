### Matching engine with <1ns (~800as) average per order fill and/or insert

Uses preallocated on the stack order storage with local "wink-out" allocator and superfast deterministic bitmap linear search
